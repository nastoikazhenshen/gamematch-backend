spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5439/gamematch_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:your_password}

app.jwt.secret=${JWT_SECRET:change-me-to-at-least-32-characters}
